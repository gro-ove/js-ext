// ==Jsx==
// @import node
// @import next
// ==/Jsx==

node ('insertAfter', lambda (child, element){
		var next = node.next (element);
		if (next)
			element.parentNode.insertBefore (child, next);
		else
			element.parentNode.appendChild (child);

		return child;
	});
