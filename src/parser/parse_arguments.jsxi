	// 11.2 Left-Hand-Side Expressions

	function parseArguments (){
		var args = [];

		expect ('(');

		if (!match (')'))
			while (index < length){
				args.push (parseAssignmentExpression ());

				if (match (')')) 
					break;

				if (match (','))
					expect (',');
			}

		expect (')');

		return args;
	}